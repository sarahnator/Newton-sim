__pycache__/
*.py[cod]
.pytest_cache/

# Rendered videos / USD stages — regenerated by the sim scripts.
outputs/*.usd
outputs/*.mp4
!outputs/.gitkeep

# Debug frame dumps from earlier Newton-MPM investigation.
outputs/_inspect_frames*/

# Genesis auto-generated caches: baked settled particle positions and
# generated cup OBJ meshes. Both are rebuilt on demand by the pipeline.
outputs/_genesis/

# Editor / tool scratch files.
.codex
